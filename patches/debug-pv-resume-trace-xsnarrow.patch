--- a/drivers/xen/xenbus/xenbus_xs.c	2026-03-26 11:24:57.759035073 +0000
+++ b/drivers/xen/xenbus/xenbus_xs.c	2026-03-26 11:25:11.533936056 +0000
@@ -817,6 +817,12 @@
 	struct xenbus_watch *watch;
 	char token[sizeof(watch) * 2 + 1];
 
+	/* isol8 trace: bit 23 = xs_resume entry */
+	{
+		extern void isol8_trace(u64 bit);
+		isol8_trace(1ULL << 23);
+	}
+
 	xb_init_comms();
 
 	mutex_unlock(&xs_response_mutex);
